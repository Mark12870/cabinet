<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.mark12870.cabinet</id>

  <name>Cabinet</name>
  <summary>Windows VST plugins in per-plugin Wine prefixes</summary>

  <metadata_license>FSFAP</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <developer id="io.github.mark12870">
    <name>Marek Ryšavý</name>
  </developer>

  <description>
    <p>
      Cabinet packages Wine as a Flatpak so Windows VST2, VST3 and CLAP plugins can run
      on an immutable Linux system, giving every plugin its own Wine prefix — its own
      C: drive, registry and dependencies — instead of one shared prefix that every
      installer fights over.
    </p>
    <p>
      Bridging is done by upstream yabridge. Only Wine runs inside the sandbox: the
      halves the DAW loads are exported to the host, so plugins appear in a native or
      Flatpak DAW as ordinary Linux plugins.
    </p>
  </description>

  <url type="homepage">https://github.com/Mark12870/cabinet</url>
  <url type="bugtracker">https://github.com/Mark12870/cabinet/issues</url>

  <launchable type="desktop-id">io.github.mark12870.cabinet.desktop</launchable>

  <provides>
    <binary>cabinet</binary>
    <binary>cabinet-gui</binary>
  </provides>

  <screenshots>
    <screenshot type="default">
      <image>https://mark12870.github.io/cabinet/screenshots/prefixes.png</image>
      <caption>Every plugin in a Wine prefix of its own</caption>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1" />
  
  <releases>
    <release version="0.7.0" date="2026-08-21">
      <description>
        <p>Eight more plugins, and prefixes that stop writing into your home</p>
        <ul>
          <li>All six of TAL's free plugins — NoiseMaker, Filter-2, Reverb-4, Chorus-LX,
          Vocoder and Bitcrusher — as Linux builds, so none of them needs Wine</li>
          <li>FabFilter's Total Bundle: all fourteen of their plugins from one installer,
          on a 30-day trial</li>
          <li>Vital, whose download sits behind an account — Cabinet opens the login page
          and installs the file you come back with</li>
          <li>A prefix keeps its Documents, Desktop and Music inside itself instead of
          pointing them at yours, so a plugin that writes there installs everything it
          should: Serum 2 had been laying down its binary and none of the 1.4 GB of
          wavetables, presets and skins its editor needs</li>
          <li>Serum 2 installs unattended now, with no wizard to click through</li>
          <li>Where a vendor serves one always-current download there is nothing to check
          it against, and both the window and the command line say so before fetching</li>
        </ul>
        <p>A prefix made before this release is repaired the next time a plugin is
        installed into it.</p>
      </description>
    </release>

    <release version="0.6.1" date="2026-08-20">
      <description>
        <p>Small fixes to the logos</p>
      </description>
    </release>
    
    <release version="0.6.0" date="2026-08-20">
      <description>
        <p>Plugins have pages, and installs that a tar cannot do</p>
        <ul>
          <li>Every plugin in the Library gets a page of its own: a screenshot, what it is,
          who makes it, which formats it installs as and what running it costs</li>
          <li>Seven free u-he synths added — Podolski, Triple Cheese, Protoverb,
          Zebralette 3, TyrellN6, ZebraCM and BazilleCM</li>
          <li>A catalogue entry can name an install script for the plugins a plain unpack
          cannot install, and say which directory of its own a plugin keeps presets in</li>
          <li>cabinet library show tells you the same as the page does</li>
          <li>Removing a Linux plugin now asks first, and says what it will delete</li>
          <li>Downloads show how far along they are: a bar in the window, percentages on
          the command line</li>
        </ul>
        <p>Removing a u-he plugin also deletes its presets, which live in ~/.u-he.</p>
      </description>
    </release>

    <release version="0.5.0" date="2026-08-19">
      <description>
        <p>A Library page, so a plugin installs itself</p>
        <ul>
          <li>Pick a plugin and it is downloaded, checked, installed and bridged for you</li>
          <li>Windows plugins ask whether to use a prefix you have or make a new one, then
          pin the Wine their editor needs and add DXVK without being told</li>
          <li>Plugins you had to buy carry the same knowledge and ask for your installer</li>
          <li>Linux plugins need no Wine at all: VST3, CLAP and LV2 land in Cabinet's own
          directory and are linked where your DAW looks, and remove takes them out cleanly</li>
          <li>The enrol command can be copied out of the window now</li>
        </ul>
        <p>Enrolled DAWs need `cabinet enrol` run again to see Linux plugins; bridged
        Windows plugins keep working untouched.</p>
      </description>
    </release>

    <release version="0.4.1" date="2026-08-19">
      <description>
        <p>Taking DXVK out can no longer delete Wine's own Direct3D</p>
        <ul>
          <li>Turning the switch off on a prefix DXVK was never in is refused, not obeyed</li>
          <li>A prefix Wine could not repair keeps saying so, rather than reading as clean</li>
          <li>Asking for dxvk anything but on or off is refused, not taken as off</li>
        </ul>
      </description>
    </release>

    <release version="0.4.0" date="2026-08-19">
      <description>
        <p>Prefixes carry their own settings</p>
        <ul>
          <li>A sync primitive per prefix: system, esync, fsync or ntsync</li>
          <li>Environment variables per prefix, reaching bridged plugins too</li>
          <li>DXVK is a switch now, and putting it back restores the DLLs it replaced</li>
          <li>New prefix and Wine versions moved onto the pages they act on</li>
        </ul>
      </description>
    </release>

    <release version="0.3.1" date="2026-08-18">
      <description>
        <p>Fixed the external link icons</p>
      </description>
    </release>
    
    <release version="0.3.0" date="2026-08-18">
      <description>
        <p>An About page names the running build: its version, the remote it was installed
        from and whether that remote is the published repository or a local one, the bundled
        yabridge and Wine, and where Cabinet keeps its prefixes, runners and sockets. The
        same is printed by `cabinet about`.</p>
      </description>
    </release>

    <release version="0.2.1" date="2026-08-18">
      <description>
        <p>The Wine versions list reads the component index Bottles reads instead of
        GitHub's API, which rations unauthenticated callers and made the list fail with
        a message about a host it had in fact reached. More builds are listed, a failure
        says what actually went wrong, and Soda's downloads now carry a checksum.</p>
      </description>
    </release>
    <release version="0.2.0" date="2026-08-18">
      <description>
        <p>The Wine versions list is grouped into families the way Bottles groups its
        runners, and Soda joins Kron4ek as a second source. Versions are no longer
        labelled with a verdict of their own.</p>
      </description>
    </release>
    <release version="0.1.0" date="2026-08-18">
      <description>
        <p>First release under Cabinet's own version. Wine 11 and yabridge 5.1.1,
        per-plugin prefixes on downloadable Wine runners, DXVK, enrolment for
        Flatpak DAWs, and a GTK4 window beside the command line.</p>
      </description>
    </release>
  </releases>
</component>
